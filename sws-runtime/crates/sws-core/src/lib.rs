//! TODO: Centralized in-memory tag database, YAML project loader, hot reload,
//! and tag expression evaluator. This is the heart of the runtime.

pub mod tag;
pub mod project;

/// Placeholder doc-test to keep CI happy until real tests are added.
///
/// ```
/// assert_eq!(2 + 2, 4);
/// ```
pub fn _placeholder() {}

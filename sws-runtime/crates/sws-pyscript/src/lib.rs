//! TODO: PyO3 bridge for sandboxed Python scripts. Executes user scripts via
//! RestrictedPython, exposes a `sws` Python module with tag read/write API.

/// ```
/// assert_eq!(2 + 2, 4);
/// ```
pub fn _placeholder() {}

//! TODO: `Historian` trait for pluggable storage backends; SQLite default
//! implementation with ring-buffer writes optimised for low-RAM embedded targets.

pub mod sqlite;

/// ```
/// assert_eq!(2 + 2, 4);
/// ```
pub fn _placeholder() {}

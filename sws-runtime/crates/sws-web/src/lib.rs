//! TODO: Axum HTTP/WSS server, REST API endpoints, WebSocket tag streaming,
//! and static file serving for the editor bundle.

pub mod router;

/// ```
/// assert_eq!(2 + 2, 4);
/// ```
pub fn _placeholder() {}

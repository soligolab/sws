# Optional Python packages consumed by the sws-runtime via PyO3.
#
# RestrictedPython enables script sandboxing for on_press / on_release
# handlers. The engine runs unsandboxed (with a startup warning) if this
# is missing — install with:
#
#     pip install -r requirements.txt
#
# scripts/dev.sh prefers `python3` from PATH; install into the same
# interpreter PyO3 will pick up (controlled by `PYO3_PYTHON`, default
# `python3` in scripts/dev.sh).

RestrictedPython>=7.0
